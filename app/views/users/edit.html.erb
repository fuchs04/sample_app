<h1>Nutzer bearbeiten</h1>

<%= form_for(@user) do |f| %>
  <%= render 'fields', f: f %>
  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>
<div>
  <%= gravatar_for @user %>
  <%= link_to  "update Nutzerbild", "http://gravatar.com/emails", target: '_blank' %>
</div>
